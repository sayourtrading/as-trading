# دليل النشر خطوة بخطوة — AS Trading

اتبع هالخطوات بالترتيب بالضبط. كل خطوة لازم تخلص قبل التالية.

## 1) افتح مشروع Supabase (قاعدة البيانات + تخزين الصور)
1. روح ع supabase.com وسجّل حساب (فيك تستخدم GitHub تبعك لتسجيل الدخول)
2. دوس "New project"
3. اختار اسم للمشروع (مثلاً as-trading) وكلمة سر لقاعدة البيانات (احفظها بمكان آمن)
4. اختار المنطقة الأقرب (مثلاً Europe)
5. انتظر لحد ما المشروع يخلص يتجهز (كم دقيقة)

## 2) جهّز قاعدة البيانات
1. من القائمة الجانبية، افتح "SQL Editor"
2. افتح ملف `supabase-setup.sql` المرفق، انسخ محتواه كامل
3. الصقه بالـ SQL Editor ودوس Run
4. لازم يطلعلك رسالة نجاح بدون أخطاء

## 3) جهّز مكان تخزين الصور
1. من القائمة الجانبية، افتح "Storage"
2. دوس "New bucket"
3. اسم الـ bucket لازم يكون بالضبط: `vehicle-photos`
4. فعّل خيار "Public bucket"
5. احفظ

## 4) اعمل حساب الأدمن (الشخص يلي رح يدخل يدير الموقع)
1. من القائمة الجانبية، افتح "Authentication" > "Users"
2. دوس "Add user" > "Create new user"
3. حط إيميلك وكلمة سر قوية — هاد الإيميل وكلمة السر يلي رح تسجّل فيهم دخول عالموقع لاحقًا
4. لازم تفعّل خيار "Auto Confirm User" وقت الإنشاء

## 5) خد مفاتيح الاتصال
1. من القائمة الجانبية، افتح "Project Settings" > "API"
2. انسخ قيمتين:
   - **Project URL**
   - **anon public key**
3. افتح ملف `.env.local.example` المرفق، وسمّيه `.env.local` (احذف .example)
4. حط القيمتين مكانهم بالملف

## 6) ارفع الكود على GitHub
1. روح ع github.com وسجّل دخول
2. دوس "+" فوق يمين > "New repository"
3. سمّيه مثلاً `as-trading-website`، خليه Private إذا بدك، ودوس "Create repository"
4. بصفحة الـ repo الفاضية، دوس "uploading an existing file"
5. فك ضغط ملف الـ zip يلي وصلك، واسحب **كل** الملفات والمجلدات جوا (عدا .env.local — هاد ما بينرفع أبدًا لأنه فيه أسرار)
6. دوس "Commit changes"

## 7) انشر عبر Vercel
1. روح ع vercel.com وسجّل دخول بحساب GitHub تبعك
2. دوس "Add New" > "Project"
3. اختار الـ repo يلي رفعته (as-trading-website)
4. قبل ما تدوس Deploy، افتح "Environment Variables" وضيف:
   - `NEXT_PUBLIC_SUPABASE_URL` = القيمة يلي نسختها من Supabase
   - `NEXT_PUBLIC_SUPABASE_ANON_KEY` = القيمة يلي نسختها من Supabase
5. دوس "Deploy" وانتظر كم دقيقة
6. رح تحصل على رابط مباشر متل `as-trading-website.vercel.app` — الموقع شغال فعليًا هلق!

## 8) جرب الموقع
1. افتح الرابط، جرب تدخل عالـ "Staff login" بالإيميل وكلمة السر يلي عملتهن بـ Supabase
2. جرب تضيف مركبة، ارفع صورة، شوفها عالصفحة الرئيسية
3. جرب تبيعها وشوف اختفت من "Trucks for Sale"

## 9) وصّل الدومين تبعك (بعد ما تشتريه)
1. بمشروعك على Vercel، روح ع "Settings" > "Domains"
2. أضف الدومين تبعك
3. Vercel رح يعطيك سجلات DNS (A record أو CNAME) لازم تحطها بلوحة تحكم الدومين (Namecheap/GoDaddy/IDM)
4. انتظر من ساعات لحد يوم لحتى يشتغل الدومين، وSSL (https) بيصير تلقائي

---
إذا صار خطأ بأي خطوة، خد لقطة شاشة (screenshot) للرسالة وابعتلي إياها هون وأنا بساعدك تحلها.
